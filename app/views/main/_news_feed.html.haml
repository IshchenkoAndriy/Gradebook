- unless articles.nil? or articles.empty?
  %table.table
    - articles.each do |article|
      %tr
        %td= article.user.email
        - if can? :read, article
          %td= link_to article.title, article
        %td=  raw truncate(article.text, :length => 40)